# Leave unset to run on the recorded results for the four samples (real backend output,
# replayed as the six streamed frames) — no backend needed. Set it to use a live backend:
# /api goes through the Vite dev proxy (→ localhost:8000) or nginx in the compose stack.
# VITE_API_URL=/api
